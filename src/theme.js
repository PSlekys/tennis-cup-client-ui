import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#dad7cd",
    },
    secondary: {
      main: "#fff",
    },
  },
});

export default theme;
